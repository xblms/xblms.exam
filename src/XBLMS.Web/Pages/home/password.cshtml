@page
@{ Layout = "_LayoutHome"; }
@section Styles{
    <style>
        body,html {
            padding: 0;
            margin: 0;
        }

    </style>
}
<div class="rounded-0 shadow p-3 bg-white sticky-top">
    <div class="fw-bolder text-center">
        修改密码
    </div>
</div>
<div class="p-5">
      <el-form v-on:submit.native.prevent ref="form" :model="form" status-icon label-width="140px">
        <el-form-item label="账号">
            {{ user.userName }}
        </el-form-item>
        <el-form-item label="密码" prop="password" :rules="[{ required: true, message: '请输入密码' }]">
            <el-input v-model="form.password" type="password" name="password" autocomplete="off"></el-input>
        </el-form-item>
        <el-form-item label="确认密码" prop="confirmPassword" :rules="[{ required: true, message: '请输入确认密码' }, { validator: validatePass }]">
            <el-input v-model="form.confirmPassword" type="password" name="confirmPassword" autocomplete="off"></el-input>
        </el-form-item>
         <el-form-item>
                <el-button plain icon="el-icon-check" type="primary" v-on:click="btnSubmitClick">保 存</el-button>
            </el-form-item>
    </el-form>
</div>

@section Scripts{
<script src="/sitefiles/assets/js/home/password.js" type="text/javascript"></script> }